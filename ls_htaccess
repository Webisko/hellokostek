# Use PHP 8.4 for LiteSpeed
<FilesMatch "\.(php|phtml)$">
    SetHandler application/x-httpd-alt-php84
</FilesMatch>

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule . index.php [L]
</IfModule>
